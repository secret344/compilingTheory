#ifndef _LEX_BOOL_
#define _LEX_BOOL_

#define BOOL unsigned int
#define TRUE 1
#define FALSE 0

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define CompareNumMAX(a, b) ((a) > (b) ? (TRUE) : (FALSE))

#endif